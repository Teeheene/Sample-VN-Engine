plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.yaml:snakeyaml:2.0'
}

application {
    mainClass = 'Main'  // fully qualified class if in a package
}

tasks.named('run') {
    standardInput = System.in
}
